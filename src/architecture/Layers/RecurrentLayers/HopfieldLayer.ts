import {DenseLayer} from "../CoreLayers/DenseLayer";

export class HopfieldLayer extends DenseLayer {
    constructor(outputSize: number) {
        // TODO: Implement it
        super(outputSize);
    }
}
